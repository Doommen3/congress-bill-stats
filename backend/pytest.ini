[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short
markers =
    live_factual: Live end-to-end factual checks against Render + official data sources.
filterwarnings =
    ignore::DeprecationWarning
